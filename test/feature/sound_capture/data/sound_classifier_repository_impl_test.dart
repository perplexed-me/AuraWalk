import 'package:flutter_test/flutter_test.dart';
import 'package:aura_walk/feature/sound_capture/data/sound_classifier_repository_impl.dart';

void main() {
  group('SoundClassifierRepositoryImpl', () {
    test('dummy test', () {
      final repo = SoundClassifierRepositoryImpl();
      expect(repo, isNotNull);
    });
  });
} 