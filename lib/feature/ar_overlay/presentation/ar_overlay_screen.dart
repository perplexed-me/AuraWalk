import 'package:flutter/material.dart';

class AROverlayScreen extends StatelessWidget {
  const AROverlayScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('AR Overlay')), // TODO: Implement UI
      // TODO: Draw a polyline along GPS coordinates using ar_flutter_plugin
      // TODO: Color the polyline according to a list of HEX values
    );
  }
} 