# Feature Layer

This directory contains feature modules for AuraWalk, each organized by domain (e.g., sound, weather, ar, aura_palette).

Each feature contains its own data, domain, and presentation subfolders for clean separation. 